[package]
name = "db-schema"
version = "0.1.0"
edition = "2021"

[dependencies]
diesel = { workspace = true, features = ["postgres", "chrono"] }
chrono = { workspace = true }
async-graphql = { workspace = true }
